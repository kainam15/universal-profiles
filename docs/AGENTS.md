# 文档维护约束

- 用[文档导航](README.md)确定每个主题的唯一维护位置。文档解释原理与契约，执行步骤放入对应 Skill；用户命令示例可保留在根 README。
- 移动章节后修复入站链接，并保留根 `REFERENCE.md` 的旧锚点跳转。字段或参数不在兼容导航中另存副本。
- 按任务关联的主题分组，优先增加已有章节；新文档必须能从根 Agent 导航或本目录索引到达。
- `reviews/` 是历史审计证据，引用时保留日期和输入来源，不将其中状态提升为现行协议。

链接、锚点和 Skill 的验证方式见[测试指南](Testing.md#文档与-skill-检查)。
