# 测试约束

- 沿用 `unittest`、`unittest.mock` 和临时目录；模拟外部服务与硬件，测试不能启动真实矩阵、发送通知或覆盖用户配置。
- 回归断言观察目标行为；公式期望值独立推导，兼容清理测试覆盖当前实现成功与旧入口明确拒绝，不用被测函数生成期望值。
- 模块移动后 patch 函数实际查找依赖的位置；导入副作用使用隔离进程验证。
- 不为纯文档或低影响格式改动添加实现镜像式测试。范围、TUI 尺寸与硬件证据要求见[测试指南](../docs/Testing.md)。
