# 本地验证产物约束

- 本目录存放临时验证脚本、测试日志和终端截图，按任务隔离输出；不将这里的结果标作正式 profiling 数据。
- 保存证据时注明验证命令、终端尺寸和运行范围；历史截图或日志不能证明当前实现已通过。
- 可复用验证进入 `tests/`，长期结论进入对应 `docs/` 专题；临时计划不提升为 Agent 常驻指令。

证据边界见[测试指南](../docs/Testing.md)。
