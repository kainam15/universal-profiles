# AC-Prof Universal Profiler - Host-side dependencies
# (Docker containers have their own requirements per task family)

# Core
requests>=2.31
huggingface_hub>=0.23.0
socksio>=1.0.0

# Plotting (optional)
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# CV workload generation (optional, for host-side image creation)
Pillow>=10.0

# GPU energy monitoring via NVML (optional, graceful fallback if unavailable)
pynvml>=11.5.0
