[tool.ruff]
required-version = "==0.16.8"
target-version = "py310"
line-length = 100
force-exclude = true
extend-exclude = ["internal-testing", "results", "evidence"]

[tool.ruff.lint]
# 首次接入只检查基础错误；import 排序和全库格式化另行评估。
select = ["E4", "E7", "E9", "F"]
